{
  "name": "open-props-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint --fix --format detailed -v --debug",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.11.1",
    "eslint": "^9.15.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "open-props": "^1.7.7",
    "prettier": "^3.4.1"
  }
}
